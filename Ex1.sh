#!/bin/sh
#
# Ex1.sh
# Example 1 of a shell script. Do something semi-automatically using 
# a script rather than typing the details on the command line.
# (I usually call this cl.sh)
#
gfortran -o quadratic quadratic.f

exit
